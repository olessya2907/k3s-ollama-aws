---
- name: Install K3S agent (worker) and join the cluster
  ansible.builtin.shell: "curl -sfL https://get.k3s.io | K3S_URL=https://{{ hostvars['master'].master_private_ip }}:6443 K3S_TOKEN={{ hostvars['master'].k3s_token }} sh -"
  args:
    creates: /etc/systemd/system/k3s-agent.service

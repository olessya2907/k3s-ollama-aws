apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ollama
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ollama
                port:
                  number: 80
